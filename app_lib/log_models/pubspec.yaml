name: log_models
description: Data models for JSON log inspector
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.8.0 <4.0.0"
resolution: workspace

dependencies:
  equatable: ^2.0.7
  meta: ^1.11.0

dev_dependencies:
  test: ^1.26.3
  flutter_lints: ^6.0.0
